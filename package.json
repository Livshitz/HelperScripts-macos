{
  "name": "myscripts",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "debug1": "nodemon --watch 'src/**/*.ts' --exec tsx --inspect -r dotenv/config api/helpers/localServer.ts",
    "debug2": "node --inspect -r ts-node/register ${1}",
    "debug": "nodemon --watch 'src/**/*.ts' --exec tsx --inspect ${1}"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "tsx": "^4.16.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "libx.js": "^4.0.1"
  }
}